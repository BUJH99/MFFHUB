import { AdminSection } from '@/components/sections/AdminSection';

export default function AdminPage() {
  return (
    <main className="min-h-screen bg-slate-50 text-slate-950">
      <AdminSection standalone />
    </main>
  );
}
